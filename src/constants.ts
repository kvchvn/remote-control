export const WS_PORT = 8080;

export const BASE_URL = `ws://localhost:${WS_PORT}`;

export const ERRORS = {
  noParams: 'Params is expected to be passed.',
};

export const PRINT_SCREEN_WIDTH = 200;

export const PRINT_SCREEN_HEIGHT = 200;
